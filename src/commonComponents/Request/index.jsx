import React from 'react';

function Request() {
  return (
    <h1> This is the Profile JSX that will be shared with both chefs and users so code can be more dry</h1>
  );
}

export default Request;
